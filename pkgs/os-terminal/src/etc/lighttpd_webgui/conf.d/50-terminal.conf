server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/api/terminal/ws" {
  proxy.server = ( "" => ( ( "host" => "127.0.0.1", "port" => 7682, "read-timeout" => 86400, "write-timeout" => 86400 ) ) )
  proxy.header = ( "upgrade" => "enable" )
}
