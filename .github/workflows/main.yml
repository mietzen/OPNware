name: Main Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'pkg-src/**'
      - 'config.yml'
      - '.github/workflows/main.yml'
      - '.github/assets/*.py'
  push:
    branches:
      - main
    paths:
      - 'pkg-src/**'
      - 'config.yml'
      - '.github/workflows/main.yml'
      - '.github/assets/*.py'
  schedule:
    - cron: "15 4 * * *"

jobs:
  discover-packages:
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Parse Packages and Config
        id: set-matrix
        run: |
          matrix=$(python ./.github/assets/build-matrix.py)
          echo "Matrix: $matrix"
          echo "matrix=$matrix" >> $GITHUB_OUTPUT

  build-packages:
    strategy:
      matrix: ${{ fromJson(needs.discover-packages.outputs.matrix) }}
      fail-fast: false
    runs-on: ubuntu-24.04
    needs: discover-packages
    env:
      ABI: ${{ matrix.abi }}
      ARCH: ${{ matrix.arch }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          path: repo

      - name: Setup GO
        uses: actions/setup-go@v5
        if: ${{ matrix.go }}
        with:
          go-version: ${{ matrix.go }}
          cache: false

      - name: Setup NodeJS
        uses: actions/setup-node@v4
        if: ${{ matrix.node }}
        with:
          node-version: ${{ matrix.node }}

      - name: Build ${{ matrix.pkg_name }}
        env:
          ABI: ${{ matrix.abi }}
          ARCH: ${{ matrix.arch }}
          PKG_NAME: ${{ matrix.pkg_name }}
        uses: mietzen/action-ansible-playbook@v2
        with:
          playbook: build_pkg.yml
          directory: repo/pkg-src/${{ matrix.pkg_name }}/
          configuration: |
            [defaults]
            callbacks_enabled = ansible.posix.profile_tasks, ansible.posix.timer
            stdout_callback = yaml
            nocows = false
            interpreter_python = auto_silent
          inventory: |
            [local]
            localhost ansible_connection=local
          options: |
            --verbose
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.run_id }}_${{ matrix.pkg_name }}_${{ matrix.arch }}_${{ matrix.abi }}
          path: dist/*
          retention-days: 1
          compression-level: 1

  build-repo:
    if: github.event_name == 'push'
    runs-on: ubuntu-24.04
    environment: github-pages
    permissions:
      pages: write
      id-token: write
    needs: build-packages
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create PKG Repository
        run: |
          for artifact in artifacts/*; do
            arch=$(basename "${artifact}" | cut -d'_' -f3)
            abi=$(basename "${artifact}" | cut -d'_' -f4)
            target_dir="${GITHUB_WORKSPACE}/FreeBSD:${abi}:${arch}/latest/All"
            mkdir -p ${target_dir}
            mv ${GITHUB_WORKSPACE}/${artifact}/*.pkg ${target_dir}/
            cat ${GITHUB_WORKSPACE}/${artifact}/packagesite_info.json >> ${GITHUB_WORKSPACE}/packagesite.yaml
            echo "" >> ${GITHUB_WORKSPACE}/packagesite.yaml
          done
          rm -rf artifacts
          tree .
          echo "hello" > index.html
