######################################################
#            GENERATED FILE DON'T CHANGE!            #
######################################################
# This file is generated by update_workflow_caller.yml
name: Build Package CaddyV2

on:
  workflow_call:
    inputs:
      abi:
        required: true
        type: string
      arch:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Package
        run: |
          echo "Building package with ABI=${{ inputs.abi }} ARCH=${{ inputs.arch }}"
          # Add your build logic here
