######################################################
#            GENERATED FILE DON'T CHANGE!            #
######################################################
# This file is generated by update_workflow_caller.yml
name: Workflow caller

on:
  workflow_call:
    inputs:
      abi:
        required: true
        type: string
      arch:
        required: true
        type: string
      pkg_name:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: dhcp-client-widget
        if: inputs.pkg_name == 'dhcp-client-widget'
        uses: ./.github/workflows/build_dhcp-client-widget.yml
        with:
          abi: ${{ inputs.abi }}
          arch: ${{ inputs.arch }}

      - name: adguard
        if: inputs.pkg_name == 'adguard'
        uses: ./.github/workflows/build_adguard.yml
        with:
          abi: ${{ inputs.abi }}
          arch: ${{ inputs.arch }}

      - name: caddyv2
        if: inputs.pkg_name == 'caddyv2'
        uses: ./.github/workflows/build_caddyv2.yml
        with:
          abi: ${{ inputs.abi }}
          arch: ${{ inputs.arch }}

      - name: upsnap
        if: inputs.pkg_name == 'upsnap'
        uses: ./.github/workflows/build_upsnap.yml
        with:
          abi: ${{ inputs.abi }}
          arch: ${{ inputs.arch }}

